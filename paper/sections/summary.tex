\section{Summary}
\label{sec:summary}

\todo{write last. Key claims to summarize:}
\begin{itemize}
  \item Exact tiling-independent multi-resolution synthesis removes the
  global FFT; peak patch becomes embarrassingly parallel over tiles and
  single-node GPU runs reach Websky resolution.
  \item Coarse-grid velocity-coherence criterion
  ($k_{\rm Nyq}\gtrsim0.3\,h\,{\rm Mpc}^{-1}$) for velocity-weighted maps;
  density products are insensitive.
  \item Deposit-position requirement for potential-weighted kernels
  (Lagrangian, not Eulerian).
  \item End-to-end reproduction of the Websky catalogue (six statistics,
  few per cent) and map suite ($\kappa$/kSZ/tSZ/CIB/ISW within
  $\sim$10--20\% over well-measured ranges).
  \item Every painted product carries an independent analytic amplitude
  anchor --- a practice we recommend after it caught two
  compensating-error scenarios during validation.
  \item Code, frozen configurations, and the eight-octant production
  dataset are public. \todo{links/DOIs}
\end{itemize}
